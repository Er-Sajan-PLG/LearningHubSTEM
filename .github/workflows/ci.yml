name: CI — STEMMA

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-learninghubstem-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: Validate Knowledge Base
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          # Full history so check_id_immutability.py can reconstruct each lhs: id's
          # identity across time and verify no id was reassigned or reused (ADR-0003).
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          pip install pyyaml jsonschema

      - name: Run authoritative verification chain (content + connections + sources + semantic tests)
        # verify_all.py is the single authoritative entry point: it runs the validator
        # (now covering content/ + connections/ + sources/), the derived-analysis scripts,
        # and the Phase-B + curation semantic/boundary test suites. CI delegates to it so
        # the gate exercises exactly what the repository's verification command does.
        run: python3 scripts/verify_all.py

      - name: Check derived exports are fresh and deterministic
        # Exports are stamped with a content hash (no wall-clock), so regeneration is
        # byte-identical when the tracked artifacts are in sync (ADR-0022 / plan v2 E5.2).
        # A non-empty diff means someone edited canonical content without committing the
        # regenerated export — fail instead of silently consuming a stale export.
        run: git diff --exit-code -- exports explorer/public/exports reports/validation-report.json

      - name: Check derived exports
        run: |
          test -f exports/knowledge.json
          echo "Derived export exists"
          ls -lh exports/

  security:
    name: Security scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        timeout-minutes: 5

      - name: Check no secrets in content
        run: |
          if grep -R -i "api_key\|secret.*=" content/ --include="*.md" | grep -v "example" | grep -v "provenance"; then
            echo "Potential secret in content"
            exit 1
          fi

  verify-docs:
    name: Verify Governance Docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Check required docs
        run: |
          for f in AGENTS.md README.md docs/GOVERNANCE.md docs/STEMMA-SPECIFICATION.md; do
            if [[ ! -f "$f" ]]; then echo "Missing $f"; exit 1; fi
          done
          echo "Docs OK"

  check-branching:
    name: Branching Strategy
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Check branch naming
        run: |
          branch="${{ github.head_ref }}"
          if [[ ! $branch =~ ^(feat|fix|docs|chore|ci|task|phase|exp|dependabot|arena)/ ]]; then
            echo "Branch '$branch' must start with feat/|fix/|docs/|chore/|ci/|task/|phase/|exp/|dependabot/|arena/"
            exit 1
          fi

  check-conventional-commits:
    name: Conventional Commits (commitlint)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check commit messages
        uses: wagoid/commitlint-github-action@v6
        with:
          configFile: commitlint.config.cjs
          failOnErrors: true
